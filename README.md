# Site Tracker MVP

A launch-tier web app to track information from arbitrary public websites.

## Goals
- Add trackers by URL
- Auto-detect fields and let users choose defaults
- Dashboard for current snapshot and history
- Generic scraping with compliance checks

## Getting started
1. npm install
2. npx prisma migrate dev --name init
3. npm run dev
